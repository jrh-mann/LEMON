/**
 * Node configuration editors — extracted from RightSidebar.tsx.
 *
 * Each editor is a self-contained component for configuring a specific
 * node type (end, subprocess, decision, calculation). They are used
 * by both RightSidebar and NodePropertiesModal.
 */

export { EndNodeConfig } from './EndNodeConfig'
export { SubprocessConfig } from './SubprocessConfig'
export { DecisionConditionEditor } from './DecisionConditionEditor'
export { CalculationConfigEditor } from './CalculationConfigEditor'
